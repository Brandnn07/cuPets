<div class="container">
    <article class="message name">
        <div class="message-header">
            <p>Messages</p>
        </div>
        <div class="message-body">
            {{log messages}}
            {{#each messages}}
                <p>sender: 
                    <a href="/message/{{from}}">{{from}}</a>
                </p>
                <div>
                    <p>
                        message: 
                    </p>
                    {{content}}
                </div>
            {{/each}}
        </div>
    </article>
</div>
<div class="container">
    <button class="button is-medium">Dashboard</button>
    <button class="button is-medium">Settings</button>
</div>
